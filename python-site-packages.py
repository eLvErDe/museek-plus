#!/usr/bin/env python
import os, sys
list1 = os.path.join("lib", "python" + str(sys.version_info[0]) + "." + str(sys.version_info[1]), "site-packages")
if len(sys.argv) > 1:
    list1 = os.path.join(list1, sys.arv[1:])

print list1
